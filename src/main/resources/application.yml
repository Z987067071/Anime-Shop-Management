server:
  port: 8080

jwt:
  secret: anime-shop-secret-key-2024-fixed-do-not-change
  expiration: 86400000

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 10MB
      location: D:/cxdownload/bishe/anime-shop/temp
  datasource:
    url: jdbc:mysql://localhost:3306/anime_shop?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      database: 0
      port: 6379
  jackson:
    deserialization:
      fail-on-unknown-properties: false
avatar:
  local:
    upload-path: D:/cxdownload/bishe/anime-shop/src/main/resources
    access-path: /avatar/
    server-url: http://localhost:8080
product:
  local:
    upload-path: D:/cxdownload/bishe/anime-shop/upload/
    access-path: /upload/
    server-url: http://localhost:8080
# 静态资源根目录（feedback/comment/communityPost 等子目录均在此下）
static:
  resources:
    base-path: D:/cxdownload/bishe/anime-shop/src/main/resources
web:
  resources:
    static-locations: file:./upload/,classpath:/static/
    add-mappings: true
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: ASSIGN_ID
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.anime.shop.entity
logging:
  level:
    com.anime.shop: debug